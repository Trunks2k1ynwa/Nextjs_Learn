import Loading from '@/components/atoms/Loading';
import React from 'react';

const LoadingUI = () => {
  return (
    <div>
      <Loading />
    </div>
  );
};

export default LoadingUI;
